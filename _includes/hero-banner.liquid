---
---

<!-- Hero Banner Component -->
<section class="hero-banner">
  <div class="hero-banner-content">
    <h1 class="hero-banner-title">{{ include.title | default: "OptimAI-Lab" }}</h1>
    {% if include.subtitle %}
      <p class="hero-banner-subtitle">{{ include.subtitle }}</p>
    {% endif %}
  </div>
</section>